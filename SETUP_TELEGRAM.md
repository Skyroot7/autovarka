# 📱 Настройка Telegram уведомлений

Этот гайд покажет, как настроить получение уведомлений о заказах в Telegram.

---

## 📋 Шаг 1: Создание Telegram бота

1. **Откройте Telegram** и найдите бота **@BotFather**
2. **Отправьте команду**: `/newbot`
3. **Введите имя бота** (например: "Автоварка Уведомления")
4. **Введите username бота** (например: "autovarka_orders_bot")
   - Username должен заканчиваться на `bot`
   - Пример: `autovarka_orders_bot`

5. **BotFather выдаст вам TOKEN** - скопируйте его!
   - Выглядит примерно так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

---

## 📋 Шаг 2: Получение Chat ID

### Вариант А: Через специального бота (проще)

1. Найдите в Telegram бота **@userinfobot**
2. Нажмите **START** или отправьте любое сообщение
3. Бот ответит вашим **Chat ID** (это число, например: `123456789`)
4. **Скопируйте это число** - это ваш Chat ID

### Вариант Б: Через вашего бота

1. Найдите своего бота по username (который вы создали в Шаге 1)
2. Нажмите **START** или отправьте `/start`
3. Откройте в браузере:
   ```
   https://api.telegram.org/bot<ВАШ_TOKEN>/getUpdates
   ```
   Замените `<ВАШ_TOKEN>` на токен из Шага 1
4. В ответе найдите `"chat":{"id":123456789}` - это ваш Chat ID

---

## 📋 Шаг 3: Настройка переменных окружения

Откройте файл `.env.local` и добавьте:

```env
# Telegram уведомления
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

**Замените:**
- `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz` на ваш **BOT TOKEN** из Шага 1
- `123456789` на ваш **CHAT ID** из Шага 2

---

## 📋 Шаг 4: Перезапуск сервера

Перезапустите сервер разработки:

```bash
# Остановите сервер (Ctrl+C)
# Запустите снова
npm run dev
```

---

## ✅ Готово!

Теперь при каждом новом заказе вы будете получать уведомление в Telegram!

---

## 🧪 Тестирование

1. Перейдите на сайт: http://localhost:3000
2. Добавьте товар в корзину
3. Оформите заказ
4. Проверьте Telegram - должно прийти уведомление! 📬

---

## 📱 Формат уведомления

Вы будете получать сообщения вида:

```
🛒 НОВЕ ЗАМОВЛЕННЯ №ORDER-1234567890

👤 Клієнт:
├ Ім'я: Іван Петренко
├ Телефон: +380636815090
├ Email: info@autovarka.com.ua
└ Адреса: Київ, вул. Хрещатик 1

📦 Товари:
1. Автомобільна мультиварка 12V
   ├ Кількість: 2 шт
   └ Ціна: 1299 грн

💰 Загальна сума: 2598 грн

📅 Дата: 21.12.2025, 14:30:25

━━━━━━━━━━━━━━━━━
🔔 Не забудьте обробити замовлення!
```

---

## 🔧 Расширенные настройки

### Отправка в несколько чатов

Если хотите получать уведомления в нескольких чатах (например, вам и менеджеру):

1. Получите Chat ID для каждого получателя
2. В `.env.local` укажите их через запятую:

```env
TELEGRAM_CHAT_ID=123456789,987654321,555555555
```

3. Обновите код в `lib/telegramService.ts`:

```typescript
export async function sendOrderNotification(orderData: any): Promise<boolean> {
  const chatIds = process.env.TELEGRAM_CHAT_ID?.split(',') || [];
  
  if (chatIds.length === 0) {
    console.log('❌ Telegram Chat ID не настроен');
    return false;
  }

  const telegramBot = initBot();
  if (!telegramBot) {
    console.log('❌ Telegram Bot не инициализирован');
    return false;
  }

  // ... формирование сообщения ...

  try {
    // Отправка всем получателям
    await Promise.all(
      chatIds.map(chatId => 
        telegramBot.sendMessage(chatId.trim(), message, { parse_mode: 'Markdown' })
      )
    );
    console.log('✅ Уведомления отправлены в Telegram');
    return true;
  } catch (error) {
    console.error('❌ Ошибка отправки в Telegram:', error);
    return false;
  }
}
```

---

## ❓ Частые проблемы

### 1. "Telegram Bot не инициализирован"
- Проверьте, что `TELEGRAM_BOT_TOKEN` правильно указан в `.env.local`
- Убедитесь, что перезапустили сервер после изменения `.env.local`

### 2. "Chat ID не настроен"
- Проверьте, что `TELEGRAM_CHAT_ID` указан в `.env.local`
- Убедитесь, что это число без лишних символов

### 3. "Bad Request: chat not found"
- Убедитесь, что вы нажали START в боте
- Попробуйте получить Chat ID заново через @userinfobot

### 4. Сообщения не приходят
- Проверьте, что бот не заблокирован вами в Telegram
- Проверьте консоль сервера на наличие ошибок
- Убедитесь, что токен и Chat ID правильные

---

## 🚀 Деплой на hostpro.ua

При деплое на хостинг:

1. Добавьте переменные окружения в панели управления хостингом:
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`

2. Или добавьте их в файл `.env` на сервере

3. Перезапустите приложение

---

## 📞 Контакты для получения уведомлений

- Telegram: +380636815090
- Email: info@autovarka.com.ua

---

## 🎉 Готово!

Теперь вы будете мгновенно получать все заказы прямо в Telegram! 🚀

