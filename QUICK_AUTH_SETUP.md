# ⚡ Быстрая настройка безопасной авторизации

## 🎯 За 3 минуты

### 1️⃣ Сгенерируйте хеш пароля

```bash
node scripts/generatePasswordHash.js ВашНадежныйПароль123!
```

**Пример вывода:**
```
# Учетные данные администратора
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=a1b2c3d4e5f6...длинный_хеш...
ADMIN_PASSWORD_SALT=x1y2z3w4v5u6...длинная_соль...
```

### 2️⃣ Скопируйте в `.env.local`

Создайте файл `.env.local` в корне проекта:

```bash
# 🔐 Авторизация администратора
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=вставьте_хеш_из_шага_1
ADMIN_PASSWORD_SALT=вставьте_соль_из_шага_1

# 📧 Email (если нужен)
SMTP_HOST=mail.autovarka.com.ua
SMTP_PORT=465
SMTP_USER=info@autovarka.com.ua
SMTP_PASS=ваш_пароль_от_почты
EMAIL_TO=info@autovarka.com.ua

# 💬 Telegram (если нужен)
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
```

### 3️⃣ Настройте Vercel

#### A. Создайте KV базу данных:

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект `autovarka`
3. Перейдите в **Storage** → **Create Database**
4. Выберите **KV** → Название: `autovarka-kv` → **Create**

#### B. Добавьте переменные окружения:

1. **Settings** → **Environment Variables**
2. Добавьте 3 переменные:

```
ADMIN_USERNAME = admin
ADMIN_PASSWORD_HASH = (ваш хеш из шага 1)
ADMIN_PASSWORD_SALT = (ваша соль из шага 1)
```

3. Для каждой выберите: **Production**, **Preview**, **Development**
4. Нажмите **Save**

### 4️⃣ Деплой

```bash
git add .
git commit -m "Добавлена безопасная авторизация"
git push origin main
```

---

## ✅ Готово!

Теперь авторизация защищена:
- ✅ Пароли хешированы
- ✅ Сессии в Vercel KV
- ✅ HTTP-only cookies
- ✅ Никаких паролей в коде

### Войти в админку:

**Локально:** http://localhost:3000/admin  
**Production:** https://autovarka.vercel.app/admin

**Логин:** admin  
**Пароль:** ваш пароль (тот, что вы указали в шаге 1)

---

## 🔧 Для разработки (упрощенный вариант)

Если не хотите возиться с хешами для локальной разработки:

```bash
# В .env.local
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
```

⚠️ **НЕ используйте это на production!**

---

## 🆘 Проблемы?

### "Cannot find module '@vercel/kv'"
```bash
npm install
```

### "Неверное имя пользователя или пароль"
- Проверьте, что правильно скопировали хеш и соль
- Убедитесь, что нет лишних пробелов

### "Ошибка при подключении к серверу"
- Убедитесь, что Vercel KV подключен
- Проверьте переменные `KV_URL`, `KV_REST_API_URL`, `KV_REST_API_TOKEN`

---

## 📚 Полная документация

См. [SETUP_ADMIN_AUTH.md](./SETUP_ADMIN_AUTH.md)

---

**Готово за 3 минуты! 🚀**
