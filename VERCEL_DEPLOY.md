# 🚀 Развертывание сайта на Vercel

## Шаг 1: Регистрация на Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **"Sign Up"**
3. Выберите **"Continue with GitHub"** (или Email, если нет GitHub)
4. Подтвердите email

---

## Шаг 2: Загрузка проекта на GitHub (если еще не загружен)

### Вариант A: Через GitHub Desktop (проще)

1. Скачайте [GitHub Desktop](https://desktop.github.com/)
2. Установите и войдите в свой аккаунт
3. Нажмите **File → Add Local Repository**
4. Выберите папку `C:\Users\user\autovarka`
5. Нажмите **Publish repository**
6. Снимите галочку **"Keep this code private"** (или оставьте приватным)
7. Нажмите **Publish Repository**

### Вариант B: Через командную строку

```bash
cd C:\Users\user\autovarka
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/autovarka.git
git push -u origin main
```

---

## Шаг 3: Импорт проекта в Vercel

1. Войдите на [vercel.com](https://vercel.com/dashboard)
2. Нажмите **"Add New..." → Project**
3. Нажмите **"Import"** рядом с репозиторием `autovarka`
4. Vercel автоматически определит Next.js
5. **НЕ нажимайте Deploy пока!** Сначала настроим переменные окружения

---

## Шаг 4: Настройка переменных окружения

Перед деплоем нужно добавить переменные окружения:

### В интерфейсе Vercel:

1. Прокрутите вниз до раздела **"Environment Variables"**
2. Добавьте следующие переменные:

#### Переменные для Email (SMTP):

| Name | Value |
|------|-------|
| `SMTP_HOST` | `mail.autovarka.com.ua` |
| `SMTP_PORT` | `465` |
| `SMTP_USER` | `info@autovarka.com.ua` |
| `SMTP_PASS` | Ваш пароль от email |
| `EMAIL_TO` | `info@autovarka.com.ua,skd7@ukr.net` |

#### Переменные для Telegram:

| Name | Value |
|------|-------|
| `TELEGRAM_BOT_TOKEN` | Ваш токен от @BotFather |
| `TELEGRAM_CHAT_ID` | Ваш Chat ID |

> **Важно:** Для каждой переменной выберите **"Production"**, **"Preview"** и **"Development"**

---

## Шаг 5: Развертывание

1. Нажмите **"Deploy"**
2. Подождите 2-3 минуты
3. После завершения нажмите **"Visit"**
4. Ваш сайт будет доступен по адресу: `https://autovarka.vercel.app` (или похожему)

---

## Шаг 6: Настройка собственного домена autovarka.ua

### 6.1. В панели Vercel:

1. Откройте ваш проект
2. Перейдите в **Settings → Domains**
3. Нажмите **"Add"**
4. Введите `autovarka.ua`
5. Vercel покажет DNS записи, которые нужно добавить

### 6.2. В панели hostpro.ua (или где зарегистрирован домен):

Добавьте следующие DNS записи:

**Вариант 1: A записи (проще):**
```
A Record:
@ → 76.76.21.21

A Record:
www → 76.76.21.21
```

**Вариант 2: CNAME запись:**
```
CNAME Record:
www → cname.vercel-dns.com
```

> **Внимание:** DNS изменения могут занять от 5 минут до 48 часов

### 6.3. Проверка:

После обновления DNS, Vercel автоматически:
- Выпустит SSL сертификат
- Перенаправит http → https
- Настроит домен

---

## ⚠️ Важные ограничения на Vercel

### Файловая система (Read-Only)

На Vercel файловая система **только для чтения**, поэтому:

❌ **НЕ работает:**
- Сохранение новых заказов в `orders.json`
- Изменение настроек видео в админке (запись в `videoSettings.json`)
- Добавление/редактирование товаров (запись в `productsData.json`)

✅ **Работает:**
- Отображение сайта
- Email уведомления о заказах
- Telegram уведомления
- Все статические данные (товары из JSON читаются при билде)
- Просмотр товаров, корзина, оформление заказов

### Решения:

**Краткосрочное (сейчас):**
- Используйте только уведомления (Email + Telegram)
- Редактируйте товары локально, коммитьте в GitHub → авто-деплой

**Долгосрочное (рекомендуется):**
- Подключите **Vercel Postgres** (бесплатно до 256MB)
- Или используйте **MongoDB Atlas** (бесплатно до 512MB)
- Мигрируйте данные из JSON в БД

---

## 🔄 Автоматические обновления

После первого деплоя:

1. Любые изменения в коде → **git push**
2. Vercel автоматически развернет новую версию
3. Проверка изменений: https://autovarka.vercel.app

---

## 📊 Что делать после деплоя

### Проверьте работу:

1. ✅ Сайт открывается
2. ✅ Переключение языков работает
3. ✅ Все страницы доступны
4. ✅ Товары отображаются
5. ✅ Форма заказа работает
6. ✅ Email уведомления приходят
7. ✅ Telegram уведомления приходят

### Возможные проблемы:

**Проблема:** Сайт открывается, но без стилей
**Решение:** Очистите кэш браузера (Ctrl+Shift+Del)

**Проблема:** Email не отправляются
**Решение:** Проверьте переменные окружения в Vercel

**Проблема:** 404 на некоторых страницах
**Решение:** Проверьте, что все файлы загружены в GitHub

---

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте логи в Vercel: **Deployments → [ваш деплой] → Logs**
2. Напишите мне - помогу разобраться!
3. [Документация Vercel](https://vercel.com/docs)

---

## 💡 Полезные команды для локальной разработки

```bash
# Запуск локального сервера
npm run dev

# Сборка для production
npm run build

# Запуск production сборки локально
npm start
```

---

## 📝 Следующие шаги (опционально)

После успешного деплоя можно:

1. **Настроить аналитику** (Google Analytics)
2. **Добавить Sitemap.xml** для SEO
3. **Мигрировать на базу данных** для полной функциональности админки
4. **Настроить резервное копирование** данных
5. **Добавить больше языков** или функций

---

## ✅ Чеклист перед деплоем

- [ ] Проект загружен на GitHub
- [ ] Создан аккаунт на Vercel
- [ ] Добавлены переменные окружения (SMTP, Telegram)
- [ ] Проверены email пароли
- [ ] Проверен Telegram токен
- [ ] Нажата кнопка Deploy
- [ ] Сайт открывается по vercel.app ссылке
- [ ] Уведомления работают
- [ ] (Опционально) Настроен домен autovarka.ua

---

**Готовы начать? Если есть вопросы на любом этапе - пишите! 🚀**

