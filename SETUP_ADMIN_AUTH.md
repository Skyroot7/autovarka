# 🔐 Настройка безопасной авторизации администратора

## 📋 Обзор

Система авторизации использует:
- ✅ **Хеширование паролей** с использованием PBKDF2 (100,000 итераций)
- ✅ **Vercel KV** для хранения сессий
- ✅ **HTTP-only cookies** для защиты токенов
- ✅ **Серверные API routes** (пароли не передаются на клиент)
- ✅ **Автоматическое истечение сессий** (24 часа)

---

## 🚀 Быстрая настройка

### Шаг 1: Генерация хеша пароля

Запустите скрипт для генерации безопасного хеша:

```bash
node scripts/generatePasswordHash.js ваш_надежный_пароль
```

**Пример:**
```bash
node scripts/generatePasswordHash.js Senatorkiller777!
```

Скрипт выведет что-то вроде:

```
# Учетные данные администратора
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=a1b2c3d4e5f6...
ADMIN_PASSWORD_SALT=x1y2z3w4v5u6...
```

### Шаг 2: Локальная разработка

Создайте файл `.env.local` в корне проекта:

```bash
# ========================================
# Авторизация администратора (БЕЗОПАСНО)
# ========================================

# Имя пользователя администратора
ADMIN_USERNAME=admin

# Хеш пароля (сгенерирован скриптом)
ADMIN_PASSWORD_HASH=ваш_хеш_из_скрипта
ADMIN_PASSWORD_SALT=ваша_соль_из_скрипта

# ========================================
# ИЛИ для разработки (НЕ для production!)
# ========================================
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=admin123
```

### Шаг 3: Настройка Vercel KV

1. Перейдите в Vercel Dashboard
2. Откройте ваш проект `autovarka`
3. Перейдите в **Storage** → **Create Database**
4. Выберите **KV** (Key-Value Store)
5. Название: `autovarka-kv`
6. Регион: выберите ближайший к вашим пользователям
7. Нажмите **Create**

Vercel автоматически добавит переменные окружения:
- `KV_URL`
- `KV_REST_API_URL`
- `KV_REST_API_TOKEN`
- `KV_REST_API_READ_ONLY_TOKEN`

### Шаг 4: Добавление учетных данных на Vercel

1. Перейдите: **Settings** → **Environment Variables**
2. Добавьте три переменные:

| Variable Name | Value | Environment |
|---------------|-------|-------------|
| `ADMIN_USERNAME` | admin | Production, Preview, Development |
| `ADMIN_PASSWORD_HASH` | *ваш хеш* | Production, Preview, Development |
| `ADMIN_PASSWORD_SALT` | *ваша соль* | Production, Preview, Development |

3. Нажмите **Save**

### Шаг 5: Деплой

```bash
git add .
git commit -m "Добавлена безопасная авторизация"
git push origin main
```

Vercel автоматически задеплоит изменения.

---

## 🔒 Безопасность

### Что реализовано:

✅ **Хеширование паролей**
- PBKDF2 с SHA-512
- 100,000 итераций
- Уникальная соль для каждого пароля

✅ **Безопасные сессии**
- Токены хранятся в Vercel KV
- HTTP-only cookies (недоступны JavaScript)
- Secure флаг в production
- SameSite strict защита
- Автоматическое истечение через 24 часа

✅ **Защита API**
- Серверные API routes
- Проверка сессий на каждом запросе
- Логирование попыток входа

### Рекомендации:

1. **Используйте сложный пароль:**
   - Минимум 12 символов
   - Буквы (A-Z, a-z)
   - Цифры (0-9)
   - Специальные символы (!@#$%^&*)

2. **Регулярно меняйте пароль:**
   - Каждые 3-6 месяцев
   - После подозрительной активности

3. **Не комитьте секреты в git:**
   - `.env.local` уже в `.gitignore`
   - Никогда не пушьте пароли в репозиторий

4. **Включите 2FA на Vercel:**
   - Account Settings → Security → Enable 2FA

---

## 📝 API Endpoints

### POST `/api/auth/login`

Авторизация администратора.

**Request:**
```json
{
  "username": "admin",
  "password": "your_password"
}
```

**Response (успех):**
```json
{
  "success": true,
  "message": "Авторизация успешна"
}
```

**Response (ошибка):**
```json
{
  "success": false,
  "message": "Неверное имя пользователя или пароль"
}
```

### GET `/api/auth/session`

Проверка активной сессии.

**Response (авторизован):**
```json
{
  "authenticated": true,
  "username": "admin"
}
```

**Response (не авторизован):**
```json
{
  "authenticated": false,
  "message": "Нет активной сессии"
}
```

### POST `/api/auth/logout`

Выход из системы.

**Response:**
```json
{
  "success": true,
  "message": "Вы вышли из системы"
}
```

---

## 🔧 Troubleshooting

### Проблема: "Ошибка при подключении к серверу"

**Решение:**
1. Проверьте, что Vercel KV подключен
2. Убедитесь, что переменные `KV_*` установлены
3. Перезапустите dev сервер: `npm run dev`

### Проблема: "Сессия истекла"

**Решение:**
- Это нормально - сессии истекают через 24 часа
- Просто войдите заново

### Проблема: "Неверное имя пользователя или пароль"

**Решение:**
1. Убедитесь, что `ADMIN_USERNAME` совпадает
2. Проверьте, что вы правильно скопировали хеш и соль
3. Перегенерируйте хеш: `node scripts/generatePasswordHash.js новый_пароль`

### Проблема: Cannot find module '@vercel/kv'

**Решение:**
```bash
npm install @vercel/kv
```

---

## 🆕 Смена пароля

1. Сгенерируйте новый хеш:
```bash
node scripts/generatePasswordHash.js новый_пароль
```

2. Обновите переменные в Vercel:
   - Settings → Environment Variables
   - Обновите `ADMIN_PASSWORD_HASH` и `ADMIN_PASSWORD_SALT`

3. Перезапустите деплоймент:
   - Deployments → Latest → Redeploy

---

## 📊 Мониторинг

### Проверка активных сессий в Vercel KV:

1. Перейдите в Storage → autovarka-kv
2. Найдите ключи с префиксом `session:`
3. Можете удалить сессии вручную при необходимости

### Команды для локальной проверки:

```bash
# Проверка сессии
curl http://localhost:3000/api/auth/session -b "admin_session=YOUR_TOKEN"

# Выход
curl -X POST http://localhost:3000/api/auth/logout -b "admin_session=YOUR_TOKEN"
```

---

## 🎯 Что дальше?

После настройки авторизации рекомендую:

1. ✅ Настроить уведомления о входах (Telegram/Email)
2. ✅ Добавить rate limiting для защиты от брутфорса
3. ✅ Настроить логирование попыток входа
4. ✅ Включить HTTPS для production
5. ✅ Регулярно проверять логи Vercel

---

## 💡 FAQ

**Q: Можно ли использовать обычный пароль без хеша?**
A: Да, для разработки можно использовать `ADMIN_PASSWORD=admin123`, но для production ОБЯЗАТЕЛЬНО используйте хеш.

**Q: Как добавить второго администратора?**
A: Сейчас система поддерживает одного администратора. Для нескольких пользователей нужно расширить систему (создать таблицу пользователей в KV).

**Q: Сессии хранятся в памяти или БД?**
A: В Vercel KV (Redis-like база данных), поэтому сессии сохраняются между перезапусками.

**Q: Что если забуду пароль?**
A: Сгенерируйте новый хеш и обновите переменные на Vercel.

---

## 📚 Дополнительная информация

- [Vercel KV Documentation](https://vercel.com/docs/storage/vercel-kv)
- [Next.js API Routes](https://nextjs.org/docs/api-routes/introduction)
- [OWASP Password Storage Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html)

---

**Автор:** AI Assistant  
**Дата:** 2026-02-04  
**Версия:** 1.0
